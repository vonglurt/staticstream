[package]
name = "staticstream-cli"
description = "sstr: record a stream into a Static Stream file, and play it back as a stream"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "sstr"
path = "src/main.rs"

[dependencies]
staticstream = { path = "../staticstream" }
staticstream-ytq = { path = "../staticstream-ytq" }
