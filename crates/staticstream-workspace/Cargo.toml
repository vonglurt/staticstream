[package]
name = "staticstream-workspace"
description = "sstr-workspace: a terminal Workspace -- Browser, Shelf, Inspector, Transcript and Services -- over folders of streams"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "sstr-workspace"
path = "src/main.rs"

[dependencies]
staticstream = { path = "../staticstream" }
staticstream-ytq = { path = "../staticstream-ytq" }
